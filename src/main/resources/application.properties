spring.application.name=Taskaz
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update

# CORS Configuration - Add your frontend URLs here (comma-separated)
# For local development, defaults are already set
# For production, set this as environment variable: cors.allowed.origins=https://your-frontend.vercel.app,https://your-frontend.netlify.app
cors.allowed.origins=http://localhost:5173,http://localhost:3000
